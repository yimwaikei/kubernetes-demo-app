apiVersion: v1
kind: Service
metadata:
  name: demo-frontend
  namespace: demo
spec:
  selector:
    app: demo-frontend
  ports:
    - port: 5173
      targetPort: 80
      nodePort: 30080
  type: NodePort
